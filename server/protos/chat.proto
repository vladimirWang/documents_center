syntax = "proto3";

package chat;

service ChatService {
  rpc Ask(ChatRequest) returns (ChatResponse);
}

message ChatRequest {
  string question = 1;
  string session_id = 2;
  int32 user_id = 3;
}

message ChatResponse {
  int32 code = 1;
  string message = 2;
  string answer = 3;
}
